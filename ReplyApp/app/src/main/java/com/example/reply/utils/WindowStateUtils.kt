package com.example.reply.utils

enum class ReplyNavigationType  {
    BOTTOM_NAVIGATION,
    NAVIGATION_RAIL,
    PERMANENT_NAV_DRAWER
}

enum class ReplyContentType {
    LIST_ONLY, LIST_AND_DETAIL
}